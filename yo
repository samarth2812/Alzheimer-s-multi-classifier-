const TruncatedTitle = ({ title }) => (
  <Tooltip title={title} arrow placement="top">
    <Box
      sx={{
        maxWidth: 300, // control width
        whiteSpace: "nowrap",
        overflow: "hidden",
        textOverflow: "ellipsis",
      }}
    >
      <Typography variant="body2" fontWeight="bold">
        {title}
      </Typography>
    </Box>
  </Tooltip>
);

<TruncatedTitle title={row.title} />
