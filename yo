const shouldShowApproval = (
  pageType: string,
  department: string,
  user: { role: string }
): boolean => {
  return (
    pageType === "latest" ||
    pageType === "rejected" ||
    (pageType === "closure" &&
      !(department === "COMPLIANCE" && user.role === "COMPLIANCE_MAKER"))
  );
};
