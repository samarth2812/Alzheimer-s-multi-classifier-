//Fetch all indents
indentRouter.get(
  "/:role/:tab/:department?",
  requireUser,
  authorize([PERMISSIONS.IBA]),
  getIndents
);

          return `${baseUrl}/api/v1/indents/${role}/created/${searchParam}?${pagination}`;
        case "latest":
