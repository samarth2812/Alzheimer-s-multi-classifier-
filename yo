{(moduleId === "indents" ? item.status : item.finalStatus) && (
  <Box sx={{ display: "flex", alignItems: "center" }}>
    {formatStatusDisplay(moduleId === "indents" ? item.status : item.finalStatus)}
  </Box>
)}
