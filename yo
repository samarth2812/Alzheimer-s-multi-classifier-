          ...({item.assignees.length > 0} 
          {
            <Button
              startIcon={<Mail fontSize="small" />}
              size="small"
              onClick={handleSendMailToAllAssignees}
              sx={animatedButtonStyle}
            >
              Send Email to All Users
            </Button>
          }
