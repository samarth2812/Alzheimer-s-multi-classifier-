type FieldChange =
  | { value: any } // For CREATE or DELETE
  | { from: any; to: any }; // For UPDATE

type Modifications = {
  fields?: Record<string, FieldChange>;
  workflow?: { fromStep: string; toStep: string };
  attachments?: { fileName: string }[];
};
