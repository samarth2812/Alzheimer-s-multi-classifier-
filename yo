<DialogTitle sx={{ p: 0 }}>
  <Box display="flex" alignItems="center" justifyContent="space-between" px={2} pt={2}>
    <Stack direction="row" spacing={2} alignItems="center">
      <Button onClick={onClose} size="small" variant="text">←</Button>
      <Avatar {...stringAvatar(user?.name || "User")} />
    </Stack>
    <Stack direction="row" spacing={2} alignItems="center">
      <SearchableDropdown
        name="commType"
        options={commTypeLabels}
        value={formData.commType}
        onChange={handleChange}
        placeholder="Comm. Type"
        style={{ minWidth: 150 }}
        size="small"
      />
      <IconButton>
        <MoreVertIcon />
      </IconButton>
      {initialData?.isReply && (
        <IconButton>
          <ReplyIcon />
        </IconButton>
      )}
    </Stack>
  </Box>
</DialogTitle>
